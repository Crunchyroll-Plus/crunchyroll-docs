getRecommendations
===========

#### I'm guessing it gives recommendations on what you should watch next.

```http
GET /recommendations/v1/next/android/${contentId}

# Request Headers
Authorization: Bearer ${TOKEN}
```